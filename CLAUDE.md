# VQMS2.0 项目说明

## 项目概述

VQMS（Voltage Quality Management System）= 基于 **RuoYi-Vue**（前后端分离版，Spring Security + JWT + Redis）的 AVC 母线电压质量监测与考核系统。

- 代码库：`myRuoYi-Vue-springboot3/`（若依 Spring Boot 3 版，标准 ruoyi-admin/common/framework/system/quartz/generator/ui 七模块）
- 业务：对接东北能源监管局"两个细则"（2024-09-04 印发，2024-10-01 施行），重点 AVC/电压/无功考核分析
- 政策原文与提炼口径：`docs/政策口径/`（`_extract_2024.txt` 为 2024 版全文提取，可 grep 检索，含页码标记）

## 单位规约（全项目强制）

| 量 | 规范单位 | 存储 |
|---|---|---|
| 电压 | **kV** | decimal |
| 无功功率 | **kvar** | decimal |
| 有功 / 容量 | **kW** | decimal |
| 合格率 / 投运率 | **%** | decimal |
| 时间（聚合） | **分钟数** | int |

**时区**：所有时间一律北京时间（UTC+8 / `Asia/Shanghai`），全项目统一。

**换算注意**：政策原文用"万千瓦/万千乏"，与规范单位差 10⁴（如 ±1 万千乏 = 10000 kvar；1.5 千伏 = 1.5 kV）。

## 政策依据核心口径（附件6 AVC，2024 版 p47-49）

- **投运率** = AVC 投运时间 / 并网运行时间 ×100%（扣除电网原因退出时间）；合格线 **99%**；缺额每百分点 `额定容量 × 0.02 分/万千瓦`
- **调节合格率** = 执行合格点数 / 发令次数 ×100%；合格判据：主站电压/无功指令下达后 **1 分钟内**调整到合格区间；合格线 **100%**；缺额同上 `0.02 分/万千瓦`
- **合格区间**：500kV ±1.5kV；220kV ±1kV 或机组无功偏差 ±1 万kvar（风光储 ±0.5 万kvar）；66kV 及以下 ±1% 额定电压 或 ±0.4 万kvar
- **结算**：1 分 = 1000 元；同一事件多条款取最大；月度结零按分类上网电量返还

### 免考判定（两条独立路径，不能混用）

1. **投运率免责**：AVC 退出时段的退出原因 ∈ {电网原因} → 从分母扣除时间（数据：AVC 投退 yx 信号 + 退出原因标注）
2. **调节合格率免责**（附件6 §三）：全部 AVC 闭环无功设备在**正确方向**（电压偏低→发出 / 偏高→吸收）顶到**各自极限**仍不达标 → 该时段免考（数据：设备级无功遥测 Q vs 双向极限配置；发电机类极限随 P 变的 P-Q 曲线、逆变器型 ±√(S²−P²)、电容/电抗单向）

**三状态模型**：每个发令点记 执行合格 / 不合格-非免考 / 不合格-免考；**剔除法**记账（免考点同时移出分子分母、不进罚）。

### VQMS 细化分档（附件6 无此规定，5 分钟阈值外部依据待确认）

不合格（>1 分钟）按响应时长分档：**[1,5) 分钟 = 调节快速性考核；≥5 分钟 = 调节经济性考核**；阈值现场可整定。

### 相关联考核（正文条款）

- 第 26 条 母线电压：季度电压曲线为依据；"AVC 主站闭环调节控制的并网主体免于考核"（需识别 AVC 投退状态）
- 第 27 条 SVG/SVC/调相机：投入自动可用率 99%，每降 1pp 月考 0.1 分/万千瓦（风光储 6 个月过渡期）
- 辅助服务细则：基本无功 = 迟相 PF≥0.85 发出 / 进相 PF≥0.97 吸收（义务）；有偿无功 300 元/万千乏时

## 注意事项

- `docs/政策口径/东北区域发电厂并网运行管理实施细则_2023.pdf` 文件名有误导：正文载明 2020-10-01 执行，**实为 2020 版（113 号文）**，仅作历史对比；实现一律以 **2024 版**为准
- 仓库根 `.gitignore` 含 `*.zip` 规则，需入库的 zip 要 `git add -f`
- `myRuoYi-Vue-springboot3` 的 7 个 ruoyi-* 模块目录设了 Windows 只读属性（attrib +R）；构建前需解除：
  ```bash
  cd /c/work/VQMS2.0/myRuoYi-Vue-springboot3 && for m in ruoyi-quartz ruoyi-system ruoyi-ui ruoyi-admin ruoyi-common ruoyi-framework ruoyi-generator; do (cd "$m" && MSYS_NO_PATHCONV=1 attrib -R /S /D '*') && (cd .. && MSYS_NO_PATHCONV=1 attrib -R "$m"); done
  ```
